SITE_TIMEZONE=Africa/Cairo
# # 数据库连接配置（nGen）
NGEN_HOST=10.25.35.208
NGEN_PORT=3306
NGEN_USER=box
NGEN_PASSWORD=your_ngen_password
NGEN_DB_NAME=hutchisonports
# # 数据库连接配置（Cactus）
CACTUS_HOST=10.25.35.209
CACTUS_PORT=3306
CACTUS_USER=box
CACTUS_PASSWORD=your_cactus_password
CACTUS_DB_NAME=kpidata2.0

DATALOG_HOST=10.25.35.209
DATALOG_PORT=3306
DATALOG_USER=root
DATALOG_PASSWORD=your_datalog_password
DATALOG_DB_NAME=dagster_pipelines
# ============================================================
# SMTP 邮件配置（多个邮箱地址请用逗号分隔）
# ============================================================
# 产线推荐：内网 SMTP Relay（避免容器出公网受限导致超时）
# - Relay: 10.25.4.70:25
# - 加密: STARTTLS
# - 发件人白名单: 必须使用 cactus@hutchisonportsabuqir.com
# - 无密码；允许外发到外部域
SMTP_HOST=10.25.4.70
SMTP_PORT=25
SMTP_SSL=False
SMTP_STARTTLS=True
SMTP_USER=cactus@hutchisonportsabuqir.com
SMTP_PASSWORD=
SMTP_MAIL_FROM=cactus@hutchisonportsabuqir.com
SMTP_TIMEOUT=30

# 告警收件人（多个用逗号分隔）
ALERT_EMAIL_TO=xiyan.zhou@westwell-lab.com

# ============================================================
# Airflow Web UI 管理员账号（示例：请在实际部署时修改）
# ============================================================
AIRFLOW_ADMIN_USER=admin
AIRFLOW_ADMIN_PASSWORD=change_me
AIRFLOW_ADMIN_EMAIL=admin@port.com

# 业务阈值（暂存）
# cycle时间误差允许范围（秒）
THRESHOLD_TIME_DIFF=300
# 检查批处理大小
BATCH_SIZE=50000

AIRFLOW_UID=1000

# ============================================================
# InfluxDB 配置（Ground Truth Validation 新增）
# ============================================================
INFLUX_URL=http://10.105.64.37:8086
INFLUX_TOKEN=your_influxdb_token_here
INFLUX_ORG=your_organization
INFLUX_BUCKET=your_bucket
INFLUX_TIMEOUT=30000

# ============================================================
# Map API 配置（Ground Truth Validation 新增）
# ============================================================
MAP_API_URL=http://10.105.66.20:1234/api/v1/annotate/batch
MAP_PORT=AQCTMap_20251121V1.0
MAP_API_TIMEOUT=30
MAP_API_USE_CACHE=true
#冷启动日期配置
DATALOG_LOGIC_CHECK_START_DATE=2025-12-18
DATALOG_NGEN_CHECK_START_DATE=2025-12-18